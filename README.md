# Calc JS

## A basic Calculator with keyboard on! (just focus the input). You can press your keyboard numbers and they will show up in the screen, erase a character by using Backspace & show result by pressing Enter.

### Note: The Calculator was developed using an internal JavaScript Function (eval), so it might throw some errors depending on your input. Ex: 8(( could result in an error.
